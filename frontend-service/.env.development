# Local development — points to gateway running via docker-compose on port 6061
# or directly at localhost:8080 if running without docker-compose
VITE_API_URL=http://localhost:6061/api
VITE_CONTACT_API_URL=http://localhost:6061/api/notifications/contact
