# Production profile — all values MUST come from environment variables
spring:
  data:
    redis:
      host: ${REDIS_HOST}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      ssl:
      enabled:true 


logging:
  level:
    root: INFO
    org.springframework.cloud.gateway: INFO
    com.portfolio.gateway: INFO
